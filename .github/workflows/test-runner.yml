name: Test Self Hosted Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, linux, x64, docker, harbor, lan]

    steps:
      - name: Show System Info
        run: |
          uname -a
          whoami
          pwd

      - name: Test Docker Access
        run: |
          docker version
          docker ps
