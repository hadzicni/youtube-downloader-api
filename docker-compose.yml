services:
  ytapi:
    build: .
    container_name: youtube_downloader_api
    restart: unless-stopped
    ports:
      - '8000:8000'
    env_file:
      - .env
    volumes:
      - ./downloads:/downloads
